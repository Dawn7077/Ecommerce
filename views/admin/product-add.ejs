<!-- product-add.ejs -->
<%- include("../../views/partials/admin/header") %>
<head>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/cropperjs@1.5.13/dist/cropper.min.css">
    <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>

  
</head>
<style>
   .error-message{
       color: red;
   }


       .thumbnails-container {
           display: flex;
           overflow-x: auto;
       }

       .image-cropper {
            display: none;
        }


       .thumbnail {
           margin-right: 10px;


       }
</style>
   <section class="content-main">
       <div class="row">
           <div class="col-9">
               <div class="content-header">
                   <h2 class="content-title">Add New Product</h2>
               </div>
           </div>
           <div class="col-lg-12">
               <div class="card mb-4">
                   <div class="card-body">
                       <form method="post" action="/admin/addProducts" enctype="multipart/form-data"
                           onsubmit="return validateForm()
">
                           <div class="mb-4">
                               <label for="product_name" class="form-label">Product Name</label>
                               <input type="text" placeholder="Type here" name="productName"
                                   class="form-control border" id="product_name">
                               <div id="productName-error" class="error-message"></div>
                           </div>
                           <div class="col-lg-4 mb-4">
                               <label class="form-label">Brand</label>
                               <select class="form-select border" name="brand">
                                <% for(let i=0; i<brand.length; i++ ){ %>
                                   
                                
                                       <option value="<%=brand[i].brandName %>">
                                           
                                        <%= brand[i].brandName %>

                                         
                                       </option>
                                       
                                     <% } %>
                               </select>
                               <div id="brand-error" class="error-message"></div>
                           </div>
                           <div class="mb-4">
                               <label class="form-label">Full description</label>
                               <textarea placeholder="Type here" id="descriptionId" name="description" class="form-control border"
                                   rows="4"></textarea>
                               <div id="description-error" class="error-message"></div>
                           </div>
                           <div class="mb-4">
                            <label class="form-label">Highlights(Specs)</label>
                            <div id="highlights-container"></div>
                            <button type="button" class="btn btn-sm btn-primary" onclick="addHighlights()">Add</button>
                           </div>
                           <div class="row">
                               <div class="col-lg-4">
                                   <div class="mb-4">
                                       <label class="form-label">Regular price</label>
                                       <input placeholder="$" name="regularPrice" type="text"
                                           class="form-control border">
                                           <div id="regularPrice-error" class="error-message"></div>
                                   </div>
                               </div>
                               <div class="col-lg-4">
                                   <div class="mb-4">
                                       <label class="form-label">Sale price</label>
                                       <input placeholder="$" name="salePrice" type="text" class="form-control border">
                                      
                                   </div>
                                   <div id="salePrice-error" class="error-message"></div>
                               </div>
                               
                           </div>
                           <div class="row">
                              
                           <!-- Prvious color & quantity box

                               <div class="col-lg-4">
                                   <div class="mb-4">
                                       <label class="form-label">Color</label>
                                       <input  name="color" type="text" class="form-control border">
                                   </div>
                                   <div id="color-error" class="error-message"></div>
                               </div> 

                               <div class="col-lg-4">
                                   <div class="mb-4">
                                       <label class="form-label">Quantity</label>
                                       <input placeholder="" name="quantity" type="text" class="form-control border">
                                       <div id="quantity-error" class="error-message"></div>
                                   </div>
                               </div>

                               -->
                            
                           </div>
                           
                           <!-- variant section -->
                            <button type="button" class="btn btn-sm btn-primary mb-3" onclick="addVariantRow()">Add Variant</button>
                           <div id="variantContainer"></div>
                           <script>
                            function addVariantRow(){
                              const div = document.createElement('div')
                              div.classList.add('row','mb-2')
                              div.innerHTML = `
                                <div class="col-3"><input type="text" name="color[]" class="form-control" placeholder="Color"></div>
                                <div class="col-3"><input type="number" name="size[]" min=1 class="form-control" placeholder="Size"></div>
                                <div class="col-3"><input type="number" name="stock[]" min=1  class="form-control" placeholder="Stock"></div>
                                <div class="col-3"><button type="button" class="btn btn-danger" onclick="this.parentElement.parentElement.remove()">Remove</button></div>
                              `
                              document.getElementById('variantContainer').appendChild(div)
                            }
                           </script>



                           <div class="card mb-4">
                               <div class="card-body">
                                   <div class="row gx-2">
                                       <div class="col-sm-6 mb-3">
                                           <label class="form-label">Category</label>
                                           <select class="form-select border" style="width: 200px;" name="category">
                                                <% for(let i=0; i<cat.length;i++){ %>

                                                   <option value="<%=cat[i].name %>">
                                                       <%=cat[i].name %>
                                                   </option>
                                                   <% } %>
                                           </select>
                                           <div id="category-error" class="error-message"></div>
                                       </div>
                                   </div>
                               </div>
                           </div>
                           <div class="card mb-2">
                               <div class="card-header">
                                   <h4>Choose images</h4>
                               </div>
                               <div class="border row">
                                   <div id="addedImagesContainer" class="thumbnails-container"></div>
                               </div>
                                                        <div class="col-6">
                                                               <div class="card-body align-items-center" style="margin-bottom: 20px;">
                                                                <img src="" alt="" id="imgView1">
                                                                <input class="form-control" type="file" name="images" id="input1"
                                                                        accept="image/png, image/jpeg, image/jpg"
                                                                        onchange="initCropper(event, 1)">
                                                                <div id="images-error" class="error-message"></div>
                                                                </div>

                                                                <div class="image-cropper align-items-center" style="display:none;">
                                                                <img src="" id="croppedImg1" alt="">
                                                                <button type="button" id="saveButton1" class="btn-sm btn-primary">Save</button>
                                                                </div>

                                                           </div>
                                                          
                                                           <div class="col-6">
                                                               <div class="card-body align-items-center" style="margin-bottom: 20px;">
                                                                <img src="" alt="" id="imgView2">
                                                                <input class="form-control" type="file" name="images" id="input2"
                                                                        accept="image/png, image/jpeg, image/jpg"
                                                                        onchange="initCropper(event, 2)">
                                                                <div id="images-error" class="error-message"></div>
                                                                </div>

                                                                <div class="image-cropper  align-items-center" style="display:none;">
                                                                <img src="" id="croppedImg2" alt="">
                                                                <button type="button" id="saveButton2" class="btn-sm btn-primary">Save</button>
                                                                </div>

                                                           </div>
                                                          
                                                           <div class="col-6">
                                                                <div class="card-body align-items-center" style="margin-bottom: 20px;">
                                                                    <img src="" alt="" id="imgView3">
                                                                    <input class="form-control" type="file" name="images" id="input3"
                                                                            accept="image/png, image/jpeg, image/jpg"
                                                                            onchange="initCropper(event, 3)">
                                                                    <div id="images-error" class="error-message"></div>
                                                                    </div>

                                                                    <div class="image-cropper align-items-center" style="display:none;">
                                                                    <img src="" id="croppedImg3" alt="">
                                                                    <button type="button" id="saveButton3" class="btn-sm btn-primary">Save</button>
                                                                    </div>

                                                           </div>

                                                           <div class="col-6">
                                                                <div class="card-body align-items-center" style="margin-bottom: 20px;">
                                                                    <img src="" alt="" id="imgView4">
                                                                    <input class="form-control" type="file" name="images" id="input4"
                                                                            accept="image/png, image/jpeg, image/jpg"
                                                                            onchange="initCropper(event, 4)">
                                                                    <div id="images-error" class="error-message"></div>
                                                                    </div>

                                                                    <div class="image-cropper   align-items-center"  style="display:none;">
                                                                    <img src="" id="croppedImg4" alt="">
                                                                    <button type="button" id="saveButton4" class="btn-sm btn-primary">Save</button>
                                                                    </div>

                                                           </div>
                                                          
                                                            
                          
                                                       </div>
                           </div>
                           <div>
                               <button class="btn btn-md rounded font-sm hover-up"  type="button" onclick="validateAndSubmit()">Publish</button>
                               <!-- <button class="btn btn-md rounded font-sm hover-up"  type="submit">Publish</button> -->
                           </div>
                       </form>
                   </div>
               </div>
           </div>
       </div>
   </section>
 
 <script src="https://cdn.jsdelivr.net/npm/cropperjs@1.5.13/dist/cropper.min.js"></script>
 
 <script>



    document.addEventListener('DOMContentLoaded', () => {
    document.querySelectorAll('.image-cropper').forEach(el => {
        el.style.display = 'none';
    });
    });


     console.log("Cropper global check:", typeof Cropper);  
    let croppers = {};  

// Initialize cropper  
function initCropper(event, index) {
  const file = event.target.files[0];
  if (!file) {
    console.log('No file selected for input' + index);
    return;
  }

  console.log('Initializing cropper for input' + index);

  const imgView = document.getElementById('imgView' + index);
//   const cropContainer = imgView.closest('.row').querySelector('.image-cropper');
  const cropContainer = imgView.parentElement.nextElementSibling; 
  const cropImage = document.getElementById('croppedImg' + index);
  const saveButton = document.getElementById('saveButton' + index);
  const input = document.getElementById('input' + index);

  //  preview image
  const previewURL = URL.createObjectURL(file);
  imgView.src = previewURL;
  imgView.style.maxWidth = '300px';
  imgView.style.maxHeight = '300px';
  imgView.style.display = 'block';

  // crop container
  cropContainer.style.display = 'flex';
  cropContainer.style.flexDirection = 'column';
  cropContainer.style.gap = '10px';
  
  cropImage.src = previewURL;
  cropImage.style.maxWidth = '100%';
  cropImage.style.maxHeight = '400px';

  // Destroy existing cropper  
  if (croppers[index]) {
    console.log('Destroying existing cropper for input' + index);
    croppers[index].destroy();
    delete croppers[index];
  }

  // waiting for image to load before initializing Cropper
  cropImage.onload = function() {
    console.log('Image loaded, creating cropper for input' + index);
    
    try {
      croppers[index] = new Cropper(cropImage, {
        aspectRatio: 1,
        viewMode: 1,
        autoCropArea: 1,
        zoomable: true,
        scalable: true,
        ready: function() {
          console.log('✓ Cropper ready for input' + index);
        }
      });
    } catch (error) {
      console.error('✗ Error creating cropper:', error);
    }
  };

  // Save cropped image handler
  saveButton.onclick = function () {
    console.log('Save button clicked for input' + index);
    
    const cropper = croppers[index];
    if (!cropper) {
      alert('Cropper not initialized. Please wait for the image to load.');
      return;
    }

    try {
      // Get cropped canvas
      const canvas = cropper.getCroppedCanvas({ 
        width: 440, 
        height: 440,
        imageSmoothingEnabled: true,
        imageSmoothingQuality: 'high'
      });

      if (!canvas) {
        alert('Failed to crop image');
        return;
      }

      // Update preview with cropped image
      imgView.src = canvas.toDataURL('image/jpeg', 0.9);

      // Convert canvas to blob and create file
      canvas.toBlob((blob) => {
        if (!blob) {
          alert('Failed to create image blob');
          return;
        }

        const fileName = `cropped-${Date.now()}-${index}.jpg`;
        const croppedFile = new File([blob], fileName, { type: 'image/jpeg' });
        
        // Replace input files
        const dataTransfer = new DataTransfer();
        dataTransfer.items.add(croppedFile);
        input.files = dataTransfer.files;

        console.log('✓ Cropped file saved:', fileName, 'Size:', blob.size, 'bytes');
        
        // Hide crop container and destroy cropper
        cropContainer.style.display = 'none';
        cropper.destroy();
        delete croppers[index];
        
      }, 'image/jpeg', 0.9);

    } catch (error) {
      console.error('✗ Error saving cropped image:', error);
      alert('Error processing image: ' + error.message);
    }
  };
}

// Thumbnail  
function handleFileSelect(event) {
  const addedImagesContainer = document.getElementById('addedImagesContainer');
  if (!addedImagesContainer) return;
  
  addedImagesContainer.innerHTML = '';
  const files = event.target.files;
  
  for (let i = 0; i < files.length; i++) {
    const thumb = document.createElement('img');
    thumb.src = URL.createObjectURL(files[i]);
    thumb.style.width = '50px';
    thumb.style.height = '50px';
    thumb.style.objectFit = 'cover';
    thumb.style.marginRight = '5px';
    thumb.style.border = '1px solid #ddd';
    thumb.style.borderRadius = '4px';
    addedImagesContainer.appendChild(thumb);
  }
}

// Attach listeners to all image inputs
document.addEventListener('DOMContentLoaded', function() {
  console.log('Attaching event listeners to image inputs');
  
  ['input1', 'input2', 'input3', 'input4'].forEach((id, i) => {
    const el = document.getElementById(id);
    if (el) {
      el.addEventListener('change', (e) => {
        console.log(`File selected for ${id}`);
        initCropper(e, i + 1);
        if (i === 0) handleFileSelect(e); // Only first input shows thumbnails
      });
      console.log(`✓ Event listener attached to ${id}`);
    } else {
      console.warn(`✗ Element ${id} not found`);
    }
  });
});

// Form validation
function validateForm() {
    console.log('Validating form...');
    clearErrorMessages();

    const name = document.querySelector('[name="productName"]').value.trim();
    const description = document.getElementById('descriptionId').value.trim();
    const brand = document.querySelector('[name="brand"]').value;
    const regularPrice = parseFloat(document.querySelector('[name="regularPrice"]').value);
    const salePrice = parseFloat(document.querySelector('[name="salePrice"]').value);
    // const quantity = parseInt(document.querySelector('[name="quantity"]').value);
    // const color = document.querySelector('[name="color"]').value.trim();
    const category = document.querySelector('[name="category"]').value;
    const images = document.getElementById('input1').files;

    





    let valid = true;

    if (!name) {
        showError('productName-error', 'Please enter a product name');
        valid = false;
    }
    
    if (!description) {
        showError('description-error', 'Please enter a description');
        valid = false;
    } else if (!/^[a-zA-Z\s.,'!&()-]+$/.test(description)) {
        showError('description-error', 'Description should only contain alphabetic characters and basic punctuation');
        valid = false;
    }

    if (!brand) {
        showError('brand-error', 'Please select a brand');
        valid = false;
    }

    if (!category) {
        showError('category-error', 'Please select a category');
        valid = false;
    }
    
    if (isNaN(regularPrice) || regularPrice <= 0) {
        showError('regularPrice-error', 'Enter a valid regular price greater than 0');
        valid = false;
    }
    
    if (isNaN(salePrice) || salePrice < 0) {
        showError('salePrice-error', 'Enter a valid sale price (0 or greater)');
        valid = false;
    }
    
    if (!isNaN(regularPrice) && !isNaN(salePrice) && regularPrice < salePrice) {
        showError('regularPrice-error', 'Regular price must be greater than sale price');
        valid = false;
    }

    //variant
    const variantRows = document.querySelectorAll('#variantContainer .row')
    let variantError =false
    let variantMsg =''

    variantRows.forEach(row=>{
      const color = row.querySelector('[name="color[]"]').value.trim()
      const size = row.querySelector('[name="size[]"]').value.trim();
      const stock = row.querySelector('[name="stock[]"]').value.trim();

      if(!color || !size || !stock){
        variantError= true
        valid = false;
      }
      if(stock<=0 ){
        variantError= true
        valid = false;
        variantMsg='Stock must be greater than 0'
      }
      if(size<=0 ){
        variantError= true
        valid = false;
        variantMsg='size must be greater than 0'
      }

    })

    if (variantError) {
      Swal.fire({
        icon: 'error',
        title: 'Variant error',
        text: variantMsg || 'All variant fields (color, size, stock) must be filled.',
      });
      return false;
    }



    
    // if (isNaN(quantity) || quantity < 0) {
    //     showError('quantity-error', 'Enter a valid quantity (0 or greater)');
    //     valid = false;
    // }
    
    // if (!color) {
    //     showError('color-error', 'Please enter a color');
    //     valid = false;
    // }
    
    // if (images.length === 0) {
    //     showError('images-error', 'Please upload at least one image');
    //     valid = false;
    // }
    let totalImages = 0;
    const imgInputs = [
        document.getElementById('input1'),
        document.getElementById('input2'),
        document.getElementById('input3'),
        document.getElementById('input4')
    ]
    imgInputs.forEach(input => {
        if (input.files && input.files.length > 0) {
            totalImages ++;
        }
    }); 
    if (totalImages < 3) {
        showError('images-error', `Please upload at least three image ,You uploaded ${totalImages} images.`);
        valid = false;
    }

    if (valid) {
        console.log('✓ Form validation passed');
    } else {
        console.log('✗ Form validation failed');
         Swal.fire({
          icon: 'error',
          title: 'Variant error',
          text: 'All variant fields (color, size, stock) must be filled.',
        });
        return false;
    }

    return valid;
}

function showError(id, msg) {
  const errorEl = document.getElementById(id);
  if (errorEl) {
    errorEl.innerText = msg;
    errorEl.style.display = 'block';
  }
}

function clearErrorMessages() {
  document.querySelectorAll('.error-message').forEach(e => {
    e.innerText = '';
    e.style.display = 'none';
  });
}

async function validateAndSubmit() {
  console.log("/addProducts form submission triggered");
  
  // checking if any croppers are still active
  const activeCroppers = Object.keys(croppers).length;
  if (activeCroppers > 0) {
    alert(`Please save all cropped images before submitting. ${activeCroppers} image(s) are still being edited.`);
    return false;
  }
  
  if (validateForm()) {
    console.log('✓ Submitting form...');
    const form = document.forms[0] 
    const formData = new FormData(form);

    const response = await fetch('/admin/addProducts', {
      method: 'POST',
      body: formData
    });
    const result = await response.json();

    if (result.status) {
      Swal.fire({
        icon: 'success',
        title: 'Product added successfully',
        text:result.message,
        showConfirmButton: false,
        timer: 1500
      }).then(() => {
        window.location.reload();
      });

 
    } else {
      Swal.fire({
        icon: 'error',
        title: 'Error adding product',
        text: result.message,
      });
    }



  } else {
    console.log('✗ Form validation failed, not submitting');
    return
  }
}

function addHighlights(value=''){
  const container = document.getElementById('highlights-container');
  const div = document.createElement('div');
  div.classList.add('d-flex','mb-2')
  div.innerHTML=`
  <input type="text" name="highlights" class="form-control border" value="${value}" placeholder="Enter highlight/spec" />
  <button type="button" class="btn btn-sm btn-danger ms-2" onclick="this.parentElement.remove()">Remove</button>
  `
  container.appendChild(div);
}

</script>

<style>
   .error-message{
       color: red;
   }

   .thumbnails-container {
       display: flex;
       overflow-x: auto;
   }

   .thumbnail {
       margin-right: 10px;
   }
   
   /* Added proper cropper container styling */
   .image-cropper {
        width: 100%;
        height: 500px;
        display: none; /* remove !important */
        margin: 20px 0;
        background: #f5f5f5;
        border: 2px solid #ddd;
        border-radius: 4px;
        flex-direction: column;
    }

   
   .image-cropper img {
       width: 100%;
       height: 100%;
       object-fit: contain;
       display: block;
   }
   
   .image-cropper button {
       margin-top: 10px;
   }
</style>
  
<%- include("../../views/partials/admin/footer") %>
 

